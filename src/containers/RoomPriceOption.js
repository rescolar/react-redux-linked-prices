import  { connect } from 'react-redux'
import RoomPriceOption from '../components/RoomPriceOption'

const mapStateToProps = (state) => {
  return {
  }
}

const mapDispatchToProps = (dispatch) => {
  return {
  }
}

export default connect(mapStateToProps, mapDispatchToProps)(RoomPriceOption)

